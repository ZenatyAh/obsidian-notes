---
source:
---
---
# Course Goals :

- What Next Js & why would we use it ?
- Routing, Pages & Server Components 
- Fetching & Sending Data
- Styling, Image upload & Managing Page Metadata

---
# Setup Project Next Js :

## **خطوات إعداد مشروع Next.js**

لإنشاء مشروع Next.js، يجب أن يكون لديك أولاً بيئة **Node.js** مُثبتة (الإصدار 18.17 أو أحدث)، لأن Next.js يعتمد عليها.

### **1. الأمر الأساسي للإنشاء**

نستخدم الأمر الرسمي **`create-next-app`**، والذي يقوم تلقائيًا بإنشاء بنية المجلدات الأساسية وتثبيت جميع التبعيات (Dependencies) الضرورية.

سنقوم بتشغيل هذا الأمر في الطرفية (Terminal) لتتمكن الأداة من طرح أسئلة التهيئة عليك:

Bash

```
npx create-next-app@latest
```

> **ملاحظة:** `npx` هي أداة لتشغيل حزم Node.js مؤقتًا دون الحاجة لتثبيتها مسبقاً بشكل عام.

---

### **2. أسئلة التهيئة (Configuration)**

بمجرد تشغيل الأمر، ستطرح عليك الأداة سلسلة من الأسئلة التي تحدد بنية مشروعك. الإجابات الشائعة هي التي بين قوسين `( )`، وعادة ما تختارها بالضغط على `Enter`:

- **? What is your project named?** (اسم مجلد المشروع)
    
- **? Would you like to use TypeScript?** (هل تريد استخدام TypeScript؟)
    
- **? Would you like to use ESLint?** (أداة لضمان جودة الكود)
    
- **? Would you like to use Tailwind CSS?** (إطار عمل CSS)
    
- **? Would you like to use `src/` directory?** (هل تفضل وضع الأكواد المصدر في مجلد `src`؟)
    
- **? Would you like to use App Router (recommended)?** (جهاز التوجيه الجديد في Next.js)
    
- **? Would you like to customize the default import alias?** (اختصار للاستيراد)
    

---

### **تدريب بسيط (صح وخطأ)**

**السؤال:** هل يجب عليك تثبيت إطار عمل **React** بشكل منفصل قبل تشغيل أمر `npx create-next-app@latest`؟

**الإجابة:** **خطأ**

---
**ما هو الفرق الرئيسي بين اختيار استخدام TypeScript واختيار JavaScript في مشروع Next.js ؟** 🧐

اختيار **TypeScript (TS)** هو قرار حكيم ومناسب لـ Next.js. كما ذكرت، الميزة الرئيسية هي إضافة **الأنواع (Types)** التي تمنحك ميزات إضافية لمنع الأخطاء.

إليك جدول يوضح الفرق الأساسي في بيئة Next.js:

| **الميزة**                | **JavaScript (JS)**                                          | **TypeScript (TS)**                                                                |
| ------------------------- | ------------------------------------------------------------ | ---------------------------------------------------------------------------------- |
| **تعريف الخصائص (Props)** | لا يوجد تعريف إلزامي.                                        | يجب تعريف خصائص المكونات (Props) وأنواعها (مثل string، number) بوضوح.              |
| **اكتشاف الأخطاء**        | يتم اكتشاف الأخطاء غالباً أثناء **تشغيل** التطبيق (Runtime). | يتم اكتشاف الأخطاء أثناء **الكتابة** (Compile time) قبل التشغيل.                   |
| **الإنتاجية**             | أسرع للمشاريع الصغيرة.                                       | أبطأ قليلاً في الإعداد، ولكنه يزيد الإنتاجية والـ Refactoring في المشاريع الكبيرة. |


---

## **بنية المشروع الأساسية (App Router)** 🏗️

بعد تشغيل أمر `create-next-app` واختيار **App Router**، سيتم إنشاء الهيكل التالي (مع افتراض أننا سمينا المشروع `my-next-app`):

Bash

```
my-next-app/
├── app/                  # مجلد التوجيه (Routing) الرئيسي
│   ├── layout.tsx        # ملف الـ Root Layout (مخطط الصفحة الرئيسية)
│   └── page.tsx          # الصفحة الرئيسية (Home Page)
├── node_modules/         # التبعيات المثبتة
├── public/               # الأصول الثابتة (صور، خطوط، أيقونات)
├── package.json          # تعريف التبعيات وأوامر التشغيل
└── ... (ملفات إعداد أخرى)
```

إن مجلد **`app`** هو قلب نظام التوجيه الجديد. فكل مجلد بداخله يمثل **مساراً (Route Segment)**.

## **1. ملف `page.tsx`** 🏠

- **يمثل:** واجهة المستخدم (UI) التي يتم عرضها لمسار URL معين.
    
- **القاعدة الأساسية:** لكي يعتبر Next.js مجلدًا معينًا **صفحة** يمكن الوصول إليها عبر URL، يجب أن يحتوي هذا المجلد على ملف `page.tsx` (أو `page.jsx`).
    
- **مثالنا:** الملف الموجود في المسار `app/page.tsx` هو ما يتم عرضه عندما يزور المستخدم المسار الجذر `/`.
    

## **2. ملف `layout.tsx`** 🖼️

أملف **`layout.tsx`** هو أحد أهم الركائز في نظام **App Router** الجديد في Next.js. إنه العنصر الذي يمكّنك من بناء واجهات مستخدم (UI) مشتركة بكفاءة عالية.

## ** وظيفة ملف `layout.tsx` الأساسية** 🖼️

الوظيفة الأساسية لملف `layout.tsx` هي تعريف **التخطيط (Layout)**، وهو واجهة مستخدم مشتركة بين مجموعة من الصفحات (Pages).

### **المفاهيم الرئيسية:**

- **الواجهة المشتركة (Shared UI):** يسمح لك بوضع مكونات ثابتة (مثل شريط التنقل العلوي Header، شريط التذييل Footer، أو الشريط الجانبي Sidebar) ليتم عرضها في جميع الصفحات التي يغطيها هذا التخطيط **دون الحاجة إلى إعادة تحميلها** عند التنقل بين الصفحات.
    
- **الـ Rendering:** التخطيطات هي **مكونات خادم (Server Components)** بشكل افتراضي، مما يعني أنها تُنفذ على الخادم، مما يحسن الأداء.
    
- **التخطيط المتداخل (Nesting):** يمكن وضع ملف `layout.tsx` في أي مجلد داخل `app/`، ويصبح أي تخطيط موجود في مجلد فرعي متداخلاً داخل التخطيط الذي يسبقه.
    

## ** محتوى ملف `layout.tsx` بالتفصيل** 📑

يوجد نوعان من التخطيطات، لكن كلاهما يتبع نفس البنية الأساسية.

### **أ. التخطيط الجذري (Root Layout)**

هذا الملف **إلزامي** ويجب أن يكون موجوداً في مسار `app/layout.tsx`. إنه يحيط بـ **كامل** تطبيقك.

#### **المتطلبات الإلزامية:**

1. **استقبال `children`:** يجب أن يقبل المكون الخاص بالتخطيط خاصية `children` من نوع `React.ReactNode`، والتي تمثل المحتوى المتداخل (الصفحة أو تخطيط فرعي آخر).
    
2. **عنصرا `<html>` و `<body>`:** يجب أن يحتوي التخطيط الجذري على عناصر HTML الأساسية **`<html>`** و **`<body>`**.
    

### **ب. تعريف البيانات الوصفية (Metadata)**

يتم تعريف البيانات الوصفية (مثل العنوان، والوصف، وصورة الـ Favicon) للصفحات عبر تصدير كائن (Object) باسم **`metadata`** من ملف التخطيط أو الصفحة:

TypeScript

```
// في ملف layout.tsx أو page.tsx
export const metadata = {
  title: 'Next.js App',
  description: 'Generated by Next.js',
};
```

---

## **3. مثال كود كامل لـ `app/layout.tsx` (TypeScript)**

يوضح هذا المثال البنية الكاملة المطلوبة للتخطيط الجذري، بما في ذلك تعريف البيانات الوصفية وتضمين ملفات CSS:

TypeScript

```
// 1. استيراد ملفات الـ CSS العامة
import './globals.css';

// 2. استيراد خطوط Google (اختياري)
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

// 3. تصدير كائن البيانات الوصفية (مطلوب لـ SEO)
export const metadata = {
  title: 'مشروعي الأول في Next.js',
  description: 'هذا هو تطبيق ويب احترافي مبني باستخدام Next.js App Router.',
};

// 4. تعريف المكون الرئيسي (Root Layout)
export default function RootLayout({
  children, // 👈 الخاصية الإلزامية التي تستقبل المحتوى المتداخل
}: {
  children: React.ReactNode;
}) {
  return (
    // 5. العنصر <html> الإلزامي
    <html lang="ar"> 
      {/* 6. العنصر <body> الإلزامي وتضمين الخط المخصص */}
      <body className={inter.className}>
        
        {/* شريط التنقل المشترك في كل صفحات الموقع */}
        <header style={{ padding: '20px', backgroundColor: '#f0f0f0' }}>
          <nav>الرئيسية | من نحن | الخدمات</nav>
        </header>

        {/* 7. المحتوى المتغير (الصفحات أو التخطيطات الفرعية) */}
        <main style={{ padding: '20px' }}>
          {children} 
        </main>
        
        {/* شريط التذييل المشترك في كل صفحات الموقع */}
        <footer style={{ marginTop: '50px', borderTop: '1px solid #ccc' }}>
          حقوق النشر محفوظة © 2024
        </footer>
      </body>
    </html>
  );
}
```

---

## **4. التخطيطات المتداخلة (`app/folder/layout.tsx`)**

- إذا أنشأت ملف `layout.tsx` داخل أي مجلد فرعي (مثل `app/about/layout.tsx`)، فإنه سيتم تطبيقه **فقط** على المسار `/about` وجميع مساراته الفرعية (مثل `/about/team`).
    
- سيتم وضع هذا التخطيط المتداخل داخل خاصية `children` الخاصة بالتخطيط الجذري (`app/layout.tsx`)، مما يضمن أن الهيكلية الأكبر (مثل شريط التنقل العلوي في مثالنا) تظل ثابتة.

---

# Reserved file name Next Js :

هذه الأسماء محجوزة لأن Next.js يستخدمها لتحديد واجهة مستخدم (UI) معينة أو سلوك خاص في شجرة التوجيه.

- **`page.js`** 📄: يحدد واجهة المستخدم الفريدة (الـ UI) التي يتم عرضها لمسار (Route) معين، وهو ما يجعله قابلاً للوصول بشكل عام.
    
- **`layout.js`** 📐: يحدد واجهة مستخدم مشتركة بين المسارات الفرعية (Children Routes). يحافظ على الحالة (State) والتصرف (Behavior) ويكون غير قابل للعرض مرة أخرى (Non-re-renderable) عند التنقل بين المسارات التابعة له.
    
- **`loading.js`** ⏳: يعرض حالة تحميل فورية (Instant Loading State) من خلال عرض واجهة مستخدم بديلة أثناء جلب المحتوى الخاص بالصفحة أو التخطيط (Page or Layout).
    
- **`error.js`** 🛑: يحدد حدود الخطأ (Error Boundaries) التي تحيط تلقائيًا بالتخطيطات (Layouts) ومكونات الصفحة (Page Components)، ويسمح بعرض واجهة مستخدم للخطأ.
    
- **`not-found.js`** 🔍: يعرض واجهة مستخدم مخصصة لصفحات **404** إذا لم يتم العثور على المسار أو في حال تم استدعاء دالة **`notFound()`**.
    
- **`template.js`** 🖼️: شبيه بـ **`layout.js`**، ولكنه **يُعرض مرة أخرى** في كل مرة يتنقل فيها المستخدم. هذا مفيد لتطبيقات CSS التي تعتمد على إعادة إنشاء DOM.
    
- **`route.js`** ⚙️: يستخدم لإنشاء **واجهات برمجة تطبيقات (API routes)** بدون واجهة مستخدم، حيث يتعامل مع طلبات HTTP مثل `GET` و `POST`.
    
- **الملفات الخاصة (Special Files):**
    
    - **`global-error.js`**: يعالج الأخطاء في مستوى التخطيط الجذري (Root Layout).
        
    - **`default.js`**: يستخدم مع ميزات التوازي في Next.js.
---
# <Link> Navigation item :

هذا المكون هو العمود الفقري للتنقل (Navigation) السريع داخل تطبيقك، وهو ما يجعله يشعر بأنه تطبيق أصلي (Native App) وليس مجرد موقع ويب تقليدي.

## **ما هو مكون `<Link>`؟**

مكون **`<Link>`** في Next.js هو غلاف (Wrapper) خاص بعلامة `<a>` القياسية في HTML، ويقوم بتعزيزها بميزتين أساسيتين:

1. **التنقل من جانب العميل (Client-Side Navigation):** عند استخدام `<Link>`، لا يقوم المتصفح بإعادة تحميل الصفحة بالكامل عند النقر على الرابط، بل يقوم Next.js بتغيير مسار URL وتحديث مكونات React المطلوبة فقط.
    
2. **الجلب المسبق (Pre-fetching):** بشكل افتراضي، يقوم Next.js بجلب الكود وبيانات الصفحة المرتبطة بالرابط في الخلفية عندما يظهر الرابط على الشاشة (في Viewport). وهذا يجعل التنقل فورياً تقريباً.
    

---

### **كيفية استخدام `<Link>`**

يجب استيراد المكون من `next/link`، ويتم تمرير مسار URL إليه عبر خاصية **`href`**:

TypeScript

```
// app/components/Navigation.tsx (مثلاً)
import Link from 'next/link'; 

export default function Navigation() {
  return (
    <nav>
      {/* 1. رابط إلى الصفحة الرئيسية */}
      <Link href="/">
        الرئيسية
      </Link>
      
      {/* 2. رابط إلى صفحة حولنا (المسار الذي أنشأناه سابقاً) */}
      <Link href="/about">
        من نحن
      </Link>
    </nav>
  );
}
```

> **ملاحظة مهمة:** في الإصدارات الحديثة من Next.js، لم يعد مكون `<a>` التابع يحتاج إلى أن يكون داخله بشكل صريح كما في الماضي، بل أصبح مكون `<Link>` هو نفسه يولد علامة `<a>` مع الخصائص الضرورية.

---

### **سؤال تدريبي:**

ذكرنا أن ميزة **الجلب المسبق (Pre-fetching)** هي إحدى نقاط القوة لـ `<Link>`.

لنفترض أن لديك زر "حذف حسابي" وربطته بصفحة التأكيد. كيف يمكنك **تعطيل** ميزة الجلب المسبق لهذا الرابط تحديداً باستخدام خاصية (Prop) معينة في `<Link>`، وما هو السبب المنطقي لتعطيل الجلب المسبق في مثل هذه الحالة؟ 🤔
إجابة ممتازة وسبب منطقي جداً! 🎯

- **الخاصية الصحيحة:** هي بالفعل **`prefetch={false}`**.
    
- **السبب المنطقي:** صحيح تماماً. تعطيل الجلب المسبق يضمن أن التطبيق **لا يقوم بتشغيل** أي منطق (Logic) غير ضروري أو إجراء مكالمات API مكلفة أو غير آمنة (مثل معالجة الحسابات أو عمليات الحذف) قبل أن يؤكد المستخدم نيته فعلاً بالنقر. هذا يحسن الأداء ويوفر موارد الخادم.
    
---
# Add Favicon :
يمكنك إضافة الصورة المصغرة للموقع من خلال وضع صورة في مجلد app تسمى `icon.png` 

---
# **ما هو الاسم المستعار (Alias)؟**

الاسم المستعار (Alias) يسمح لك باستبدال مسارات الاستيراد الطويلة والمطلقة **(Relative Paths)**، مثل `../../components/Button`، بمسارات قصيرة وواضحة تبدو وكأنها مسارات مطلقة **(Absolute Paths)**، مثل **`@/components/Button`**.

### **لماذا نستخدم الـ Alias؟**

1. **الوضوح والقراءة:** يجعل كود الاستيراد نظيفاً جداً، حيث لا تحتاج لحساب عدد المجلدات التي يجب أن تعود إليها باستخدام `../`.
    
2. **الصيانة:** عند نقل ملف ما داخل بنية المجلدات، لن تحتاج إلى تحديث مسارات الاستيراد في الملفات الأخرى التي تستورده، طالما أنك تستخدم Alias ثابت (مثل `@/`).
    

---
# How to orgnize next js project :

**read this resource : [link](https://nextjs.org/docs/app/getting-started/project-structure#colocation)**

## **كيفية الإعداد في Next.js** ⚙️

يتم تعريف الاسم المستعار في ملف التهيئة الرئيسي لمشروعك، وهو **`tsconfig.json`** (بما أننا اخترنا TypeScript).

Next.js يقوم بإنشاء الاسم المستعار الافتراضي **`@/*`** الذي يشير إلى المجلد الجذر **`./`** لمشروعك. يمكنك تعديل هذا الإعداد داخل كائن **`compilerOptions`**:

### **1. تهيئة ملف `tsconfig.json`:**

ستجد أن Next.js يضيف خاصية **`paths`** داخل `compilerOptions`.

```
// tsconfig.json
{
  "compilerOptions": {
    // ... إعدادات أخرى
    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"] // 👈 هذا هو التعريف الافتراضي
    }
  }
}
```

> **توضيح:**
> 
> - `@/*`: هذا هو الاسم المستعار الذي سنستخدمه في الكود، و **`*`** هنا هو Wildcard يمثل أي شيء يتبع `@/`.
>     
> - `["./*"]`: هذا هو المسار الفعلي الذي يشير إليه الاسم المستعار، و **`./`** يشير إلى المجلد الجذر.
>     

### **2. الاستخدام في الكود (الفرق):**

إذا كان لديك مكون يسمى `Button` موجود في المسار `src/components/Button.tsx`:

|**بدون Alias (مسار نسبي)**|**مع Alias (مسار مطلق)**|
|---|---|
|`import Button from '../../components/Button';`|`import Button from '@/components/Button';`|

النسخة الثانية (باستخدام `@/`) تبقى ثابتة وواضحة بغض النظر عن موقع الملف الذي يستوردها.

---
أهلاً بك\! لقد استعنت بالوثائق الرسمية لـ Next.js لشرح كيفية تنظيم الملفات. 📄

يعتمد Next.js في نظام **App Router** على بنية قائمة على **التوجيه المعتمد على نظام الملفات (File-system based routing)**، مما يجعل هيكلة المشروع سهلة ومنطقية.

-----

## **1. المجلدات والملفات العليا (Top-Level Structure)** 🏗️

هذه هي المجلدات والملفات التي ستجدها في جذر مشروعك، ولكل منها دور محدد:

| المجلد/الملف | الوصف والوظيفة |
| :--- | :--- |
| **`app/`** | **نظام التوجيه الرئيسي:** يحتوي على جميع الصفحات، والتخطيطات، ونقاط النهاية للـ API (`route.js/ts`). يُستخدم لتعريف بنية URL. |
| **`public/`** | **الأصول الثابتة (Static Assets):** مخصص للملفات التي يجب أن تكون متاحة للعامة مباشرة، مثل الصور، والخطوط، والملفات الجذرية (مثل `favicon.ico` أو `robots.txt`). |
| **`src/`** | **مجلد المصدر (اختياري):** مجلد اختياري يمكنك استخدامه لوضع جميع أكواد التطبيق (مثل `app/`، `components/`، `lib/`) داخله لزيادة التنظيم. |
| **`next.config.js`** | ملف تهيئة Next.js (لتغيير الإعدادات المتقدمة). |
| **`package.json`** | تعريف التبعيات وأوامر التشغيل الخاصة بالمشروع. |
| **`tsconfig.json`** | ملف تهيئة TypeScript (حيث يتم تعريف الـ Alias). |

-----

## **2. اتفاقيات ملفات التوجيه (Routing Files)** 🧭

داخل مجلد **`app/`**، توجد مجموعة من الملفات ذات الأسماء المحجوزة، ولكل منها وظيفة محددة تتعلق بالمسار الذي يوجد فيه:

| اسم الملف | الوظيفة في المسار (Route Segment) | الشرح |
| :--- | :--- | :--- |
| **`page.tsx`** | **الصفحة:** الملف الوحيد الذي يعرض واجهة المستخدم (UI) لمسار URL معين. **(إلزامي لتعريف المسار)** |
| **`layout.tsx`** | **التخطيط:** واجهة مستخدم مشتركة تغلف الصفحات والمخططات المتداخلة في المجلد الحالي والمجلدات الفرعية. |
| **`loading.tsx`** | **تحميل (Loading UI):** يعرض حالة تحميل "هيكل عظمي" (Skeleton) للصفحة أثناء جلب البيانات. |
| **`error.tsx`** | **معالجة الأخطاء (Error Boundary):** يعرض واجهة مستخدم مخصصة لمعالجة الأخطاء التي تحدث داخل هذا المقطع. |
| **`not-found.tsx`** | **غير موجود (404 UI):** يعرض واجهة المستخدم عندما يتعذر العثور على المسار. |
| **`route.ts`** | **معالج المسار (API Route):** يستخدم لإنشاء نقاط نهاية للـ API (REST, GraphQL) باستخدام وظائف `GET`, `POST`, إلخ. |

-----

## **3. مفهوم التجميع (Colocation)**

**Colocation** (التجميع في نفس الموقع) هو أفضل ممارسة لتنظيم ملفاتك في Next.js، وهي ما تم الإشارة إليه في المصدر الذي قدمته.

### **ماذا يعني التجميع؟**

يعني وضع المكونات، والأكواد المساعدة (Utilities)، ومنطق الـ CSS الذي يخص **صفحة معينة** في مجلد تلك الصفحة نفسها.

**مثال تطبيقي:**

إذا كان لديك مسار للمدونة هو `/blog`، فبدلاً من وضع مكونات المدونة في مجلد عام يسمى `components/blog/PostCard.tsx`، فإنك تضعها في:

```bash
app/
└── blog/
    ├── page.tsx          # صفحة المدونة الرئيسية
    └── PostCard.tsx      # مكون بطاقة المقال (خاص بالمدونة)
```

### **استخدام المجلدات الخاصة (Private Folders)** 🔐

لتنظيم ملفات التجميع بشكل أفضل وضمان أن Next.js **لن يحاول** توجيهها كمسارات URL، يمكنك استخدام **شرطة سفلية (`_`)** في بداية اسم المجلد:

```bash
app/
└── blog/
    ├── page.tsx
    └── _components/      # 👈 مجلد خاص (Private) ليس مسار URL
        ├── PostCard.tsx
        └── AuthorInfo.tsx
    └── _lib/             # 👈 مجلد خاص لأكواد الخدمات (Utils/Logic)
        └── fetchPosts.ts
```

  * باستخدام `_components`، فإن Next.js يعلم أن هذه الملفات هي جزء من هيكل المشروع، ولكن لا يمكن الوصول إليها عبر URL مثل `/blog/_components`.
  * هذه الطريقة تحافظ على نظافة مجلد `app/` وتجعل الكود المتعلق بمسار معين سهل العثور عليه وتعديله.
---
